const express = require("express");

const {
  userConfirmed,
  trackingById,
  trackingId,
  createTracking,
  singleTracking,
  editTracking,
  deleteTracking,
  addNote,
} = require("");

const {} = require("");
const {} = require("");

const router = express.Router();

// Get all tracking numbers
router.get();

// Get tracking number by ID
router.get();

// Create a new tracking number
router.post();

// Edit a tracking number
router.put();

// Delete a tracking number
router.delete();

// Add a note to a tracking number
router.post();

// Remove a note from a tracking number
router.post();

router.param();

router.param();

module.exports();
